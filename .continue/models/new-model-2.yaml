name: Mistral via Ollama
version: 0.0.1
schema: v1
models:
  - provider: ollama
    model: mistral:latest
    name: Mistral (local)
    roles:
      - chat
      - edit
      - autocomplete
      - apply